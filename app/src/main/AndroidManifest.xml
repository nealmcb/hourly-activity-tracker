<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Health Connect permissions for reading step count data -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />

    <!-- Required for Health Connect on older API levels -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault"
        android:fullBackupContent="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declare Health Connect data types used by this app -->
        <activity
            android:name="androidx.health.connect.client.PermissionController"
            android:exported="true" />
    </application>

    <!-- Required query for Health Connect SDK availability -->
    <queries>
        <!-- Phone / tablet: standalone Health Connect APK -->
        <package android:name="com.google.android.apps.healthdata" />
        <!-- Wear OS: platform-integrated Health Connect settings activity -->
        <intent>
            <action android:name="androidx.health.ACTION_HEALTH_CONNECT_SETTINGS" />
        </intent>
    </queries>

</manifest>
